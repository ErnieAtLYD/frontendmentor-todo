import {render, screen, fireEvent} from '@testing-library/react';
import FooterArea from './FooterArea';

// render the component
// click on the "Clear completed" button
// verify the completed items are cleared

// render the component with some active and completed items
// click on the Active filter link
// verify the Active link is active
// verify only the active items are shown
